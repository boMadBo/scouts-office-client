import React from 'react';

const id = '74842';

const Test = () => {
  // let { data } = testAPI.useGetTestQuery({ id });

  const handleClick = () => {};

  return (
    <div>
      <button onClick={handleClick}>Get data</button>
      {/* <span>{data?.share.title}</span> */}
    </div>
  );
};

export default Test;
