import React from 'react';
import styles from './about.module.scss';

const About = () => {
  return (
    <section className={styles.about}>
      <div className={styles.container}>
        <h1>Scouts Office App in development...</h1>
      </div>
    </section>
  );
};

export default About;
